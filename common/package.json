{
  "name": "bwatch-common",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "tea-cup-core": "^1.0.2"
  },
  "scripts": {
    "clean": "rimraf dist && rimraf build",
    "build": "npm run clean && tsc",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "typescript": "^4.0.2"
  }
}
