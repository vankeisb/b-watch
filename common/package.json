{
  "name": "bwatch-common",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
  },
  "scripts": {
    "clean": "rimraf dist && rimraf build",
    "build": "npm run clean && tsc",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "typescript": "^4.0.2"
  }
}
